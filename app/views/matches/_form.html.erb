<%= form_with(model: match) do |form| %>
  <% if match.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
        <% match.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :team_a_id, style: "display: block" %>
    <%= form.text_field :team_a_id %>
  </div>

  <div>
    <%= form.label :team_b_id, style: "display: block" %>
    <%= form.text_field :team_b_id %>
  </div>

  <div>
    <%= form.label :played_at, style: "display: block" %>
    <%= form.text_field :played_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
