<%= form_with(model: player) do |f| %>
  <%= f.text_field :first_name %>
  <%= f.text_field :last_name  %>
  <%= f.select     :role, Player::ROLES %>
  <%= f.collection_select :team_id, Team.all, :id, :name %>
  <%= f.submit %>
<% end %>
